## 基本概念

- Python 是一个**强类型**、**动态类型**语言，其中强类型是针对对象而言，而动态类型是针对变量而言。
- Python 中一切皆对象，并且表面上看字面值也是对应类型的对象，当然实际上字面值表达式的结果是返回一个对应类型的对象。
- 所有的对象都具有特定的类型，一个对象的值需要转换为其他类型是需要显式转换的，即**强类型**特性。
- 所有的变量只是一个绑定，它可以在任何时刻指向任何类型的对象，即**动态性**；对变量执行的操作，事实上是对当前其绑定的对象所做的操作；无法追溯的对象将通过垃圾回收机制进行回收。

