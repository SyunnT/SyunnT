## Hello World!

```swift
print("Hello world!")
```



## 解析

- 和 Python 类似，全局作用域中的代码会被当作程序的入口点，因此不需要 `main()` 函数。
- 不需要在每条语句的末尾添加分号，但也可以加上
- 如果同一行有多条独立语句，每条语句间必须使用添加分号



## 运行

### 解释型

通过 `swift` 命令可以直接以解释方式执行 swift 程序，和 `python` 类似



### 编译型

通过 `swiftc` 命令，可以先将代码编译为可执行程序，然后通过双击或终端启动



