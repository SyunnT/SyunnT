- `public`：成员在整个程序内可被访问
- `private`：成员可被类的成员函数和友元访问，但是不能被使用该类的代码访问。
  派生类中的成员或友元只能通过派生类对象来访问基类的受保护成员，不能直接通过基类对象访问受保护的成员，例如：
  成员函数或友元接受一个基类的参数，在该函数内不能通过这个基类的参数访问其受保护成员；而如果成员函数或友元接受一个本派生类的参数，在该函数内就可以通过这个派生类的参数访问基类中的受保护成员。
- `protected`：派生类可访问但用户不可访问